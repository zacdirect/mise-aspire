#!/usr/bin/env bash

set -euo pipefail

# Aspire CLI is distributed as nightly builds only; there is no stable release
# channel with binary assets.  'daily' is always the recommended install.
printf "daily\n"
